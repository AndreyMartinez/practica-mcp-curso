// Tarea 16 · sidebar · filtrarMenuPorRol
// TODO: Recibe `items` (arreglo de items de menú, cada uno con `permisoRequerido`
que puede ser `null` o un string) y `permisosUsuario` (arreglo de strings).
Regresa solo los items donde `permisoRequerido` es `null`, o donde
`permisosUsuario` incluye ese permiso.
export function filtrarMenuPorRol(items, permisosUsuario) {
  throw new Error("no implementado");
}
