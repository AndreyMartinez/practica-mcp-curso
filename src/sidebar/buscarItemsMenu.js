// Tarea 24 · sidebar · buscarItemsMenu
// TODO: Regresa los items cuyo `label` incluye `query` (sin importar
mayúsculas/minúsculas). Si `query` está vacío (tras trim), regresa todos
los items sin filtrar.
export function buscarItemsMenu(items, query) {
  throw new Error("no implementado");
}
