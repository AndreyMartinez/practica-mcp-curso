// Tarea 19 · sidebar · contarNotificacionesPorItem
// TODO: Recibe `itemId` (string) y `notificaciones` (arreglo de objetos con
`itemId`). Regresa cuántos elementos de `notificaciones` tienen ese mismo
`itemId`.
export function contarNotificacionesPorItem(itemId, notificaciones) {
  throw new Error("no implementado");
}
