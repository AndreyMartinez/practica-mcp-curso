// Tarea 23 · sidebar · agruparMenuPorSeccion
// TODO: Regresa un objeto donde cada llave es un valor distinto de `seccion`,
y el valor es el arreglo de items que pertenecen a esa sección
(mismo orden relativo en que aparecen en `items`).
export function agruparMenuPorSeccion(items) {
  throw new Error("no implementado");
}
