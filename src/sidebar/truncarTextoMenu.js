// Tarea 29 · sidebar · truncarTextoMenu
// TODO: Si `texto.length <= maxLargo`, regresa `texto` tal cual. Si no, regresa
los primeros `maxLargo - 1` caracteres (sin espacios sobrantes al final)
seguidos de "…".
export function truncarTextoMenu(texto, maxLargo) {
  throw new Error("no implementado");
}
