// Tarea 17 · sidebar · marcarItemActivo
// TODO: Regresa un arreglo NUEVO (no mutar `items`) donde cada item tiene una
propiedad extra `activo: true/false` según si `item.ruta === rutaActual`.
export function marcarItemActivo(items, rutaActual) {
  throw new Error("no implementado");
}
