// Tarea 25 · sidebar · generarBreadcrumb
// TODO: Recibe `itemId` y `items` (donde cada item tiene `id`, `label` y
`parentId`, que es `null` en la raíz). Regresa un arreglo de labels desde
la raíz hasta el item (en ese orden). Si `itemId` no existe, regresa `[]`.
export function generarBreadcrumb(itemId, items) {
  throw new Error("no implementado");
}
