// Tarea 30 · sidebar · calcularItemsVisibles
// TODO: Recibe `alturaDisponible` y `alturaPorItem` (números, en px). Regresa
cuántos items completos caben: `Math.floor(alturaDisponible / alturaPorItem)`,
nunca menor a 0.
export function calcularItemsVisibles(alturaDisponible, alturaPorItem) {
  throw new Error("no implementado");
}
