// Tarea 28 · sidebar · debeMostrarDivisor
// TODO: Recibe `itemActual` e `itemAnterior` (puede ser `null` si es el primer
item de la lista). Regresa true si `itemAnterior` no es null Y su
`seccion` es distinta a la de `itemActual` (para dibujar una línea
divisoria entre secciones del menú). Si `itemAnterior` es null, regresa
false.
export function debeMostrarDivisor(itemActual, itemAnterior) {
  throw new Error("no implementado");
}
