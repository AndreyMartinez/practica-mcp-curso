// Tarea 6 · login · generarTokenMock
// TODO: Regresa un token de sesión falso: la cadena `"<email>:<timestamp>"`
codificada en base64 (usa Buffer.from(...).toString("base64")).
export function generarTokenMock(email, timestamp) {
  throw new Error("no implementado");
}
