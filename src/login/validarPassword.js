// Tarea 2 · login · validarPassword
// TODO: Regresa true si `password` tiene al menos 8 caracteres Y contiene al
menos un dígito. Cualquier otro caso, false.
export function validarPassword(password) {
  throw new Error("no implementado");
}
