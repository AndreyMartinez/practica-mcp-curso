// Tarea 8 · login · esSesionValida
// TODO: Regresa true si `ahora - timestampCreacion` es menor que `duracionMs`
(el tercer parámetro, con valor por defecto 3600000 = 1 hora).
export function esSesionValida(timestampCreacion, ahora, duracionMs = 3600000) {
  throw new Error("no implementado");
}
