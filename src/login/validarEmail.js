// Tarea 1 · login · validarEmail
// TODO: Regresa true si `email` tiene forma básica usuario@dominio.ext
(usa la expresión regular /^[^\s@]+@[^\s@]+\.[^\s@]+$/), false en cualquier
otro caso (incluyendo cadena vacía).
export function validarEmail(email) {
  throw new Error("no implementado");
}
