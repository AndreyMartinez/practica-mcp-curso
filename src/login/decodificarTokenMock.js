// Tarea 7 · login · decodificarTokenMock
// TODO: Inverso de generarTokenMock: recibe el token en base64 y regresa
`{ email, timestamp }`, donde timestamp es un Number.
export function decodificarTokenMock(token) {
  throw new Error("no implementado");
}
